## الوضع الحالي

في رأس قسم «مهام اليوم» (`src/components/AdminCustomerIntelligence.tsx` سطر 2268-2295) يوجد 3 بادجات بجانب العنوان:

| البادج | الحالة الآن |
|---|---|
| **22 متبقي** | Badge ساكن — لا يفعل شيء |
| **4 مكتمل** | Badge ساكن — لا يفعل شيء |
| **2 تمت اليوم** | زر شغّال — ينقل للتبويب |

## المطلوب

تحويل البادجين الساكنين إلى أزرار وظيفية بنفس مظهرهما الحالي تقريباً.

## التغييرات

### 1) زر «22 متبقي»
- يصبح زر داكن (foreground) قابل للضغط.
- عند الضغط:
  - يطفئ `showCompletedTasks` (ليبقى التركيز على المتبقي فقط).
  - يعمل scroll لقائمة المهام (anchor جديد `id="today-tasks-list"`).

### 2) زر «4 مكتمل»
- يصبح toggle أخضر — يبدّل بين عرض/إخفاء المهام المكتملة (نفس وظيفة زر «إظهار المكتمل» الموجود في الجهة المقابلة).
- عند التفعيل: يتغير لونه لأخضر مُمتلئ مع علامة ✓، ويعمل scroll للقائمة ليرى المستخدم المهام الظاهرة.
- عند الإطفاء: يرجع للستايل الفاتح الحالي.

### 3) إضافة anchor
- إضافة `id="today-tasks-list"` على حاوية قائمة المهام بعد رأس البطاقة، حتى يعمل الـ scroll للزرين.

### 4) زر «تمت اليوم»
- يبقى كما هو (شغّال بالفعل).

## ملف وحيد متأثر
- `src/components/AdminCustomerIntelligence.tsx` — تعديل سطور 2268-2276 + إضافة `id` على حاوية القائمة.

## لا يوجد
- لا تغييرات في قاعدة البيانات.
- لا تغييرات في منطق الفلترة أو العدّ — نفس `pendingTasksCount` و `completedTasksCount` و `showCompletedTasks` المستخدمة حالياً.
